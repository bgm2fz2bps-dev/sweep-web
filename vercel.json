{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/tab/[...path].js": {
      "regions": ["syd1"]
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
